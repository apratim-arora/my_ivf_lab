name: my_ivf_lab
description: "IVF Embryology Cycle Management"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.32.0
  drift_flutter: ^0.3.0
  flutter_riverpod: ^3.3.0
  intl: ^0.20.0
  go_router: ^14.6.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.32.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true